#pragma once

enum struct AssemblyErrors {
	CommandNotFound,
	FileNotOpen,
	NonNumeric,
	Overflow,
	NotEnoughBytes,
	MissingEnd,
	AlreadyDefined,
	Undefined
};

enum struct RuntimeErrors {

};
